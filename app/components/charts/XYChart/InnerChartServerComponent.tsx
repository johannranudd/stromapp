import { getPrices } from "@/app/utils/gets";
import { use, Suspense } from "react";

export default function InnerChart() {
  // const data = use(getPrices());
  // console.log(data);
  // console.log("InnerChartServerComponent mounted");

  // if (!data) return <div>Loading...</div>;

  return <div>InnerChart</div>;
}
